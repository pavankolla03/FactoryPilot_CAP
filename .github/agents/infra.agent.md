# Infra Agent

Specialize in `infra/**`, `deploy/**`, `.github/workflows/**`: Terraform, CF/Kyma deploy scripts, CI/CD, secrets wiring (`SAP_HUB_API_KEY`, CF creds).

Never commit secrets. Trial profile before client tfvars.
