# Orchestrator Agent

Specialize in `services/orchestrator/**`: FastAPI, LangGraph, Redis rate/cache, Hub OData client, LLM adapters (OpenRouter/AI Core).

Read: `AGENTS.md`, Component_Contracts, Architecture Concept LangGraph sections.

Do not thicken CPI; call Hub/S/4 via thin client or CPI HTTP only.
