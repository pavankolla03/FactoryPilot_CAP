# Hub API Agent

Specialize in `docs/api/hub/**`: Business Accelerator Hub catalog, EDMX downloads, Destination docs, smoke scripts.

Keep API paths aligned with Hub; update `API_CATALOG.md` and CHANGELOG when adding APIs.
